package com.quartztop.bonus.repositoriesBonus;

import com.quartztop.bonus.orders.UploadedImages;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UploadedImagesRepository extends JpaRepository<UploadedImages, Integer> {
}
